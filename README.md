# Setup

macOS 向けの開発環境と Zsh 設定をセットアップします。

```sh
bash install.sh
```

このスクリプトは Homebrew（未導入の場合）、`git`、`fzf`、および Docker・各種ブラウザ・VS Code などを導入します。

`~/.zshrc` が存在しない場合は、このリポジトリの `.zshrc` へのシンボリックリンクを作成します。既存の `~/.zshrc` は変更しません。
